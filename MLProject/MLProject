name: "MARGOHAN_ML_PROJECT"

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      data_path: {type: str, default: "dataset_preprocessing/dataset.csv"}
    command: "python modelling.py --data_path {data_path}"
